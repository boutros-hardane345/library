<%- include('./partials/head') %>
<% if (typeof currentUser !== 'undefined' && currentUser) { %>
  <%- include('./partials/sidebar') %>
  <div class="main">
<% } %>

<div style="display:flex;align-items:center;justify-content:center;min-height:100vh;flex:1;padding:40px">
  <div style="text-align:center;max-width:400px">
    <div style="font-size:60px;margin-bottom:16px">⚠</div>
    <h2 style="font-family:'Playfair Display',serif;font-size:22px;margin-bottom:8px;color:var(--accent)"><%= title %></h2>
    <p style="color:var(--text-muted);font-size:14px;margin-bottom:24px"><%= message %></p>
    <a href="/dashboard" class="btn btn-primary">Go to Dashboard</a>
  </div>
</div>

<% if (typeof currentUser !== 'undefined' && currentUser) { %>
  </div>
<% } %>
<%- include('./partials/footer') %>
